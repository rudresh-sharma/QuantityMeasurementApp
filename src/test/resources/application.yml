spring:
  application:
    name: quantity-measurement-app

  datasource:
    url: jdbc:mysql://gateway01.ap-southeast-1.prod.aws.tidbcloud.com:4000/quantitymeasurementdb?sslMode=VERIFY_IDENTITY
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: 2PtXTXe3HdVNSok.root
    password: UelmCpH2g2FOauxK

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: never

logging:
  level:
    root: INFO
    org.hibernate.SQL: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

auth:
  jwt:
    secret: 1234567890123456789012345678901234567890123456789012345678901234
    expiration-minutes: 180
  oauth2:
    redirect-uri: http://localhost:4200/oauth-success

app:
  cors:
    allowed-origin: http://localhost:4200